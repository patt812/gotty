coverage:
  output: coverage.out
  threshold: 80

report:
  path: report.json

diff:
  path: previous_report.json

comment:
  github:
    token: ${{ secrets.GITHUB_TOKEN }}
    type: pr

summary:
  if: is_pull_request

body:
  if: is_pull_request
